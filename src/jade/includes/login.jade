.minby-align
	.minby-site__container
		.minby-grid__container
			//- form#frmLogin.minby-form.minby-form--login(action='http://helborbellavitta2.com.br/minby/index.php' method='post')
			form#frmLogin.minby-form.minby-form--login(action='/minby/index.php' method='post')
				.minby-form__field
					label.fa.fa-user(for='login__username')
						span.minby-hidden E-mail
					input#login__username.minby-form__input(type='email' name='login__username' placeholder='E-mail' required)
					//- input#login__username.minby-form__input(type='email' name='login__username' placeholder='E-mail' value="eric@previewserv.com.br" required)
				.minby-form__field
					label.fa.fa-lock(for='login__password')
						span.minby-hidden Senha
					input#login__password.minby-form__input(type='password' name='login__password' placeholder='Senha' required)
					//- input#login__password.minby-form__input(type='password' name='login__password' placeholder='Senha' value="111" required)
				.minby-form__field
					button(type='submit', formnovalidate) Entrar
			//- p.minby-text--center
			//- 	| Esqueceu a senha? 
			//- 	a(href='#') Recupere agora

script.
	(function(){
		'use strict';

		function minbyAjaxSuccess(frm) {
			var r = JSON.parse(this.responseText);
			//- console.debug(r);
			if (r.success) {
				//- frm.classList.remove('submitted');
				frm.reset();
				window.location = r.url;
				return null;
			}
			alert('E-mail ou senha inválida');
			//- growl().notifica(r.title || 'Aviso', r.message, 'theNotification--theme-red');
		}

		function minbyLogin(frm) {
			var xhr;
			xhr = new XMLHttpRequest();
			xhr.onload = minbyAjaxSuccess.bind(xhr, frm);
			xhr.open('POST', frm.action);
			xhr.setRequestHeader('X-Requested-With', 'XMLHttpRequest');
			xhr.send(new FormData(frm));
		}

		function onSubmit(event) {
			var frm;
			event.preventDefault();
			frm = event.currentTarget;
			minbyLogin(frm);
		}

		var frmLogin = document.querySelector('#frmLogin');
		frmLogin.addEventListener('submit', onSubmit);
	})();
